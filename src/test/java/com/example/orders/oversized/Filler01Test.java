package com.example.orders.oversized;

import org.junit.jupiter.api.Test;

/**
 * Filler test #01. Exists only to inflate the PR file count past the
 * agent-policy.yaml fail threshold. Demonstrates the PR-size gate.
 */
class Filler01Test {
    @Test
    void noop_01() {
        // intentionally empty
    }
}
