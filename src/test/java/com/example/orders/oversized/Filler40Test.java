package com.example.orders.oversized;

import org.junit.jupiter.api.Test;

/**
 * Filler test #40. Exists only to inflate the PR file count past the
 * agent-policy.yaml fail threshold. Demonstrates the PR-size gate.
 */
class Filler40Test {
    @Test
    void noop_40() {
        // intentionally empty
    }
}
